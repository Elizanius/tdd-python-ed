name: Proyecto Python TDD
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Instalar Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Instalar dependencias (pytest)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Ejecutar tests suma
        run: tests/test_suma.py
      - name: Ejecutar tests resta
        run: tests/test_resta.py
      - name: Ejecutar tests division
        run: tests/test_div.py
      - name: Ejecutar tests multiplicacion
        run: tests/test_multi.py
